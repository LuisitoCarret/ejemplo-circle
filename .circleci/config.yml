version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:18.20
    steps:
      - checkout
      - run: npm ci || npm install   
      - run: node app.js            
      - run: npm test               

workflows:
  build:
    jobs:
      - build
